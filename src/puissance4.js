const readline = require('readline')

const a = 42
let b =12
b = b +b;

const = '
string
${a} ${b}'

const arr = [a,b];
arr.push(s);

const obj ={
    prop: 'value',
    prop2: 123,

}

for (le i = 0; i < arr.length;i++){
    console.log(arr[i]);
}